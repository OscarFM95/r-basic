x = (pi^2)/2 #Así se define una variable que aparecerá
#En enviroment

y <- cos(pi/4) #Otra forma de definir variable

sin(pi/4) + cos(pi/4) -> z #Otra forma def var

#A lo que apunta la flecha es el nombre de variable

Nombre = "Oscar" #Para definir strings entre comillas
#Tratar de no usar caracteres especiales en variables